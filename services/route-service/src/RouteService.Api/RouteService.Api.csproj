<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>RouteService.Api</RootNamespace>
    <UserSecretsId>route-service-8a3b2c1d</UserSecretsId>
    <!--
      NU1608: reported here even though this project never references Pomelo
      directly — it's a transitive restore diagnostic via the ProjectReference
      to RouteService.Infrastructure. See the matching, fully-explained
      suppression + Pomelo PackageReference comment in
      RouteService.Infrastructure.csproj for the actual rationale.
    -->
    <NoWarn>$(NoWarn);NU1608</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Serilog.AspNetCore" Version="8.0.3" />
    <PackageReference Include="Serilog.Sinks.Console" Version="6.0.0" />
    <PackageReference Include="Serilog.Enrichers.Environment" Version="3.0.1" />

    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="10.0.0" />
    <!-- Native minimal-API OpenAPI document generation, served at /openapi/v1.json. -->
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.0" />
    <!--
      Microsoft.AspNetCore.OpenApi 10.0.0 transitively pulls in
      Microsoft.OpenApi 2.0.0, which has a known high-severity DoS advisory
      (GHSA-v5pm-xwqc-g5wc / CVE-2026-49451, CVSS 7.5 — a circular schema
      reference can crash the process via stack overflow while parsing an
      OpenAPI document). Fixed in 2.7.5 for the 2.x line. Pinned directly
      here to override the transitive version — NuGet resolves the highest
      version requested across the graph. Not bumped to the 3.x line since
      that's a breaking major version change for a package this project
      doesn't reference directly; re-verify with the .NET CLI's package
      vulnerability check once network access is available, since this was
      not restored/tested in this pass.
    -->
    <PackageReference Include="Microsoft.OpenApi" Version="2.7.5" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Scalar.AspNetCore" Version="2.1.2" />

    <PackageReference Include="AspNetCore.HealthChecks.NpgSql" Version="9.0.0" />
    <PackageReference Include="AspNetCore.HealthChecks.Redis" Version="9.0.0" />
    <PackageReference Include="AspNetCore.HealthChecks.Rabbitmq.v6" Version="9.0.0" />
    <PackageReference Include="AspNetCore.HealthChecks.UI.Client" Version="9.0.0" />

    <PackageReference Include="OpenTelemetry.Extensions.Hosting" Version="1.17.0" />
    <PackageReference Include="OpenTelemetry.Instrumentation.AspNetCore" Version="1.17.0" />
    <PackageReference Include="OpenTelemetry.Instrumentation.Http" Version="1.17.0" />
    <PackageReference Include="OpenTelemetry.Instrumentation.Runtime" Version="1.17.0" />
    <PackageReference Include="OpenTelemetry.Instrumentation.EntityFrameworkCore" Version="1.17.0-beta.1" />
    <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" Version="1.17.0" />
    <PackageReference Include="OpenTelemetry.Exporter.Prometheus.AspNetCore" Version="1.17.0-beta.1" />

    <PackageReference Include="Grpc.AspNetCore" Version="2.67.0" />
  </ItemGroup>

  <ItemGroup>
    <Protobuf Include="Protos\route.proto" GrpcServices="Server" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RouteService.Application\RouteService.Application.csproj" />
    <ProjectReference Include="..\RouteService.Infrastructure\RouteService.Infrastructure.csproj" />
  </ItemGroup>

</Project>

# Production build. The container's nginx proxies /api/v1/* to the API gateway
# (service name `api-gateway`). No internal service URL is baked into the bundle.
VITE_API_BASE_URL=/api/v1
VITE_USE_MOCK_API=false

VITE_API_BASE_URL=http://localhost:8080/api/v1

# Demo mode (default true): answers every API call in-process via
# src/api/mockAdapter.ts, so `npm run dev` works standalone with no backend
# running. Set to false once real services are deployed.
VITE_USE_MOCK_API=true

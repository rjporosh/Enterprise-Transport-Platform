# Local dev. `npm run dev` proxies /api → http://localhost:8080 (the platform
# API gateway, see vite.config.ts). Start the gateway with:
#   dotnet run --project infrastructure/gateway/src/Platform.Gateway
VITE_API_BASE_URL=/api/v1
VITE_USE_MOCK_API=false

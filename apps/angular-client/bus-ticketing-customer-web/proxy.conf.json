{
  "//": "M0: single API entry point. `ng serve` proxies every /api call to the platform API gateway (YARP). The gateway then routes to the right backend service — this file no longer knows about individual services or their ports. Run the gateway locally with `dotnet run --project infrastructure/gateway/src/Platform.Gateway` (listens on http://localhost:8080), or point GATEWAY at a running gateway.",
  "/api": {
    "target": "http://localhost:8080",
    "secure": false,
    "changeOrigin": true,
    "logLevel": "debug"
  }
}
